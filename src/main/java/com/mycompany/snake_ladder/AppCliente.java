package com.mycompany.snake_ladder;

/**
 * Classe executável para iniciar a aplicação no modo cliente.
 * Esta classe serve como ponto de entrada específico para executar
 * o jogo Cobras e Escadas como cliente.
 * 
 * @author Snake_Ladder Team
 * @version 1.0
 */
public class AppCliente {
    /**
     * Método principal que inicia a aplicação no modo cliente.
     * 
     * @param args Argumentos da linha de comando
     */
    public static void main(String[] args) {
        App.launchCliente(args);
    }
}